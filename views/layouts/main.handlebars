<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homeward Bound</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cherry+Bomb+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
</head>

<body class="pgFormat text-center">
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid btn-group-vertical" role="group" aria-label="Vertical button group">
            <a class="bigTitle navbar-brand" href="/">Homeward Bound</a>
            <button class="fadeIntLeft navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="lightSpeedRight nav-item">
                        <a class="nav-link active btn btn-primary" type="button" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active btn btn-primary" type="button" aria-current="page" href="/profile">Profile</a>
                    </li>
                    {{#if logged_in}}|
                    <li class="nav-item">
                        <button class="no-button btn btn-primary me-2" type="button" id="logout">Logout</button>
                    </li>
                    {{else}}
                    <a class="nav-link btn btn-primary" type="button" href="/login">Login</a>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>

    <main class="container container-fluid mt-5">
        {{{ body }}}
    </main>
    <footer>

    </footer>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}

</body>

</html>